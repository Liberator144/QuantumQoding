/**
 * Intelligent Refactoring Module
 *
 * Provides tools for context-aware code refactoring suggestions.
 *
 * @version 1.0.0
 */

// Refactoring engine
const { RefactoringEngine } = require('./RefactoringEngine');

// Export all components
module.exports = {
  RefactoringEngine,
};
