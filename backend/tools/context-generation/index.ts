/**
 * Context-Aware Code Generation Module
 *
 * Provides tools for generating code based on context.
 *
 * @version 1.0.0
 */

// Context-aware code generator
const { ContextCodeGenerator } = require('./ContextCodeGenerator');

// Export all components
module.exports = {
  ContextCodeGenerator,
};
