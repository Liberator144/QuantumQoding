"use strict";
/**
 * Verification Module
 *
 * This module exports all verification components for ensuring quantum coherence,
 * detecting duplicates, and measuring performance across the codebase.
 *
 * @version 1.0.0
 */
const { VerificationSystem } = require('./VerificationSystem');
const coherence = require('./coherence');
const duplicates = require('./duplicates');
const performance = require('./performance');
const integration = require('./integration');
module.exports = {
    VerificationSystem,
    coherence,
    duplicates,
    performance,
    integration,
    /**
     * Create a new VerificationSystem instance
     * @param {Object} options - Configuration options
     * @returns {VerificationSystem} New VerificationSystem instance
     */
    createVerificationSystem: (options = {}) => new VerificationSystem(options),
};
